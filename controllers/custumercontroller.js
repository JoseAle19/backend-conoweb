const createOrder = (re, res) => {
    const {dateOrder,  statusOrder, transactionNumber, clientId, noteClient, customerNote, discounts } = req.body
    
  //-- Date order
  // -- Status otder
  // -- Number order
  // -- Id Client
  // -- Note client
  // -- Discounts
};
